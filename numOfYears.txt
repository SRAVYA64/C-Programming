/*14. Write a C program to convert a given integer (in days) to years, months and days, 
 assumes that all months have 30 days and all years have 365 days.*/
#include<stdio.h>
int main(){
	int days;
	printf("enter integer(in days):");
	scanf("%d",&days);
	printf("number of years is:%d",(days)/365);
	printf("number of months:%d",(days)/12);                    
	printf("number of days:%d",(days)/)                        
}
